//querrySelector

export function $(str) {
  return document.querySelector(`${str}`);
}

export function addToNumbers(a, b) {
  return a + b;
}

export function addToNumbers(a, b) {
  return a + b;
}
